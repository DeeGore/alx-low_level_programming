#include "main.h"

/**
 * mul - Multiplies two integers
 * @l: The first number to multiply
 * @k: The second number to multiply with the first number
 * Return: The result of multiplication between l param and k param
 */

int mul(int l, int k)
{
	return (l * k);
}
